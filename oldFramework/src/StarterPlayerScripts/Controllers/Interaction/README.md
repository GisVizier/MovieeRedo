# Interaction Controller

## 📦 Drag & Drop Ready

Interaction controller is in its own folder.

## Controller

- **InteractableController.lua** - Interaction system (doors, buttons, etc.)

## Usage

Loaded automatically by `Initializer.client.lua`.

## Dependencies

- Uses `Locations` for module paths
- Uses `RemoteEvents` for networking
- Uses `Config` for settings

