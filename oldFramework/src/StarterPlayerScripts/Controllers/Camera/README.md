# Camera Controller

## 📦 Drag & Drop Ready

Camera controller is in its own folder.

## Controller

- **CameraController.lua** - First-person camera system

## Usage

Loaded automatically by `Initializer.client.lua`.

## Dependencies

- Uses `Locations` for module paths
- Uses `Config` for settings

